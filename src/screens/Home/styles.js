import { StyleSheet } from "react-native";
import styled from "styled-components/native";

export const styles = StyleSheet.create({
    image: {
        height: '100%',
        width: '100%',
    },
    cardContainer: {
        marginTop: 40,
        marginLeft: 20,
        scrollMarginBottom: 240,
    },
});

export const FlexText = styled.View`

`;